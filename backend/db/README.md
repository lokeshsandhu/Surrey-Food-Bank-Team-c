# Database

This project uses PostgreSQL.

We plan to use **raw SQL** (via the `pg` driver) rather than an ORM.

- `schema.sql`: current intended schema (authoritative reference for the project)
- `migrations/`: SQL migration files (ordered)

