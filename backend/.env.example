# Server
PORT=3000
NODE_ENV=development

# Database 
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/surrey_foodbank?schema=public"

# Auth (placeholder for now)
JWT_SECRET="change-me"
JWT_EXPIRES_IN="7d"
BCRYPT_ROUNDS=12

# Base64-encoded 32-byte key (for AES-256-GCM field encryption)
FIELD_ENCRYPTION_KEY="base64-32-bytes"
