# Server
PORT=3000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:5173

# Database
# Prefer DATABASE_URL in deployed environments such as Render.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/sfb_db

# Optional discrete database settings for local development.
DB_USER=postgres
DB_HOST=localhost
DB_NAME=sfb_db
DB_PASSWORD=cpsc319
DB_PORT=5432
DB_SSL=false

# Auth
JWT_SECRET=change-me

# Base64-encoded 32-byte key (for AES-256-GCM field encryption)
FIELD_ENCRYPTION_KEY=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
