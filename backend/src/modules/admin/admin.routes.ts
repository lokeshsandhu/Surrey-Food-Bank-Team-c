/**
 * Admin routes (scaffold only).
 *
 * Intended endpoints:
 * - GET /api/admin/clients
 * - GET /api/admin/clients/:id
 * - GET /api/admin/appointments
 *
 * Intentionally left blank per request: “remove code, just want files”.
 */
