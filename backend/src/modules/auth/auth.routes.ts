/**
 * Auth routes (scaffold only).
 *
 * Intended endpoints (per design doc):
 * - POST /api/auth/register
 * - POST /api/auth/login
 * - GET|POST /api/auth/me
 *
 * Intentionally left blank per request: “remove code, just want files”.
 */
